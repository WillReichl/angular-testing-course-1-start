describe('CalculatorService', () => {
  // "specification", i.e. "test"
  it('should add two numbers', () => {
    pending(); // lets javascript know this test is not ready to be run yet
  });

  it('should add subtract numbers', () => {
    fail();
  });
});
